// all the below are expressions (they produce a value)
3 + 4
1991
true && false && !false

// statements - bigger pieces of code that are executed and don't produce a value on their own
// statements end in semicolon
// javaScript has defined rules where statements and / or expressions can be used.
//for e.g.: in template literals we can only use expressions but not statements

if (23 > 10) {
  const str = "23 is bigger";
}
